# Informações Gerais

Para conseguir levar isso para a branch que aparece publicamente, use o:

``` bash
cd /home/invalidblock/Documents/Portifolio/invalidblock/site

npm run deploy

# Isso leva o que há na pasta para a branch gh-pages e faz o deploy
```
